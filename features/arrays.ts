const carMakers: string[] = ["ford", "toyota", "chevy"];
const dates = [new Date(), new Date()];
const carByMaker = [["rfew"], ["Desa"], ["vgsd"]];

const car = carMakers.pop();
carMakers.map((car) => {
  return car;
});

const importantDates: (string | Date)[] = [new Date(), "fsaf"];
